#include <vector>
#include <array>
#include <cmath>
#include <valarray>
#include <Eigen/Dense>

vector<float> assemblage(vector<vector<vector<float>>>matLinkSpec,vector<vector<bool>> matLiaison,vector<bool>U,vector<float>F);
